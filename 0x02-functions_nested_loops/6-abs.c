#include "main.h"

/**
 * _abs - computes the abs value of an int
 * @t: The int to be computed
 *
 * Reurn: The absolute value or zero
 */

int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
